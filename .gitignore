# deps
/node_modules

# local Pi sessions for this harness
/sessions

# next
/.next/
/out/

# misc
.DS_Store
*.pem
npm-debug.log*

# typescript
*.tsbuildinfo
next-env.d.ts
.vercel
.env*
!.env.example

# private reference material (moved out of repo)
/Refs

# local agent / workspace tooling — never publish
/.agents
/.claude
/.context
/skills-lock.json

# beautiful-ui package build artifacts + local deps
/packages/*/node_modules/
/packages/*/dist/

# easter-egg video — kept on the deploy (via .vercelignore) but not published
/public/subway-surfers.mp4
